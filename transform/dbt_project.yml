name: 'transform'
version: '1.0.0'
profile: 'transform'

model-paths:    ["models"]
test-paths:     ["tests"]
macro-paths:    ["macros"]
seed-paths:     ["seeds"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  transform:

    silver:
      +materialized: table
      +format: iceberg
      +schema: silver

    gold:
      +materialized: table
      +format: iceberg
      +schema: gold